// lib de componentes
npm install styled-components

// lib para virar trabalhar com single page, não ficar recaregando a pagina toda
npm install react-router-dom

// lib para fazer o carousel mais bonito
npm install react-slick

// padronizaçao de codigo e auxia nos erros (3 opcao é a melhor)
npx eslint --init
// ajuda na prototipação
npm install prop-types
https://www.npmjs.com/package/prop-types

// back-end simulado, pronto em json-server
npm install json-server
https://github.com/typicode/json-server

// eslin - problema de quebra de linha, lf crlf
https://stackoverflow.com/questions/37826449/expected-linebreaks-to-be-lf-but-found-crlf-linebreak-style

// Rodar mais de um comando que trava o terminal ao mesmo tempo
npm install --save-dev concurrently
https://www.npmjs.com/package/concurrently


